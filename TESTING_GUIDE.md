# EngForge Phase 1 测试流程

> 目标：系统性走完所有用户路径，找出功能漏洞、数据不一致、边界崩溃。
> 方法：按模块逐个测试，每个测试用例有明确步骤和预期结果。
> 工具：浏览器 + Supabase Dashboard（看数据）+ 浏览器 DevTools Console（看报错）

---

## 准备工作

1. 确保 dev server 在运行：`http://localhost:3000`
2. 打开浏览器 DevTools（F12），切到 Console 和 Network 面板
3. 打开 Supabase Dashboard → Table Editor，准备查看数据库变化
4. 准备两个测试邮箱（如 test1@gmail.com / test2@gmail.com）

---

## 模块 A：认证系统

### A1. 注册 — 邮箱确认关闭模式

> 前提：Supabase Dashboard → Authentication → Providers → Email → Confirm email = OFF

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 打开 `http://localhost:3000/register` | 看到注册表单 |
| 2 | 填入用户名 `testuser1`、邮箱、密码 `123456` | — |
| 3 | 点击「注册」 | 按钮变为「注册中...」→ 自动跳转到 `/dashboard` |
| 4 | 检查 Supabase → Table Editor → `profiles` 表 | 应有一条新记录，username = testuser1，avatar_initials = TU |
| 5 | 检查 Console | 无报错 |

**潜在 bug**：如果触发器 `handle_new_user` 失败，auth.users 有记录但 profiles 没有 → 此时 layout.tsx 的 `getSessionUser()` 会 fallback 到 email 前缀，但 avatar_initials 会变成 "U"

### A2. 注册 — 重复邮箱

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 用 A1 同样的邮箱再次注册 | 显示红色错误提示「User already registered」|
| 2 | 检查 Console | 无未捕获异常 |

### A3. 注册 — 密码过短

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 填入密码 `123`（少于 6 位） | 浏览器原生校验阻止提交（minLength=6）|

### A4. 注册 — 邮箱确认开启模式

> 前提：Supabase Dashboard → Confirm email = ON
> 同时确认：Authentication → URL Configuration → Site URL = `http://localhost:3000`，Redirect URLs 包含 `http://localhost:3000/auth/callback`

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 用新邮箱注册 | 显示「注册成功！请查收确认邮件后登录。」|
| 2 | 去邮箱查收 | 收到 Supabase 确认邮件 |
| 3 | 点击邮件中的链接 | 跳转到 `http://localhost:3000/auth/callback` → 自动跳转 `/dashboard` |
| 4 | 如果 code_verifier 丢失（换浏览器点链接） | 跳转到 `/login` 并显示错误提示「邮箱确认失败...」|

**潜在 bug**：注册成功提示用了红色错误样式（`bg-red/10 text-red`），语义不对——应该用绿色成功样式

### A5. 登录 — 正常流程

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 退出登录，回到 `/login` | 看到登录表单 |
| 2 | 填入 A1 注册的邮箱和密码 | — |
| 3 | 点击「登录」 | 跳转到 `/dashboard` |
| 4 | 检查 TopBar 右上角 | 显示头像缩写 + 用户名 |

### A6. 登录 — 错误密码

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 填入正确邮箱，错误密码 | 显示「邮箱或密码错误」|
| 2 | 检查是否暴露 Supabase 原始错误信息 | **不应暴露**（当前代码硬编码了中文提示，✓）|

### A7. 路由保护 — 未登录访问受保护页面

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 退出登录，直接访问 `/dashboard` | 307 重定向到 `/login?redirect=/dashboard` |
| 2 | 直接访问 `/editor` | 重定向到 `/login?redirect=/editor` |
| 3 | 直接访问 `/editor/任意ID` | 重定向到 `/login` |

### A8. 路由保护 — 已登录访问认证页

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 登录后访问 `/login` | 重定向到 `/dashboard` |
| 2 | 登录后访问 `/register` | 重定向到 `/dashboard` |

### A9. 退出登录

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 点击右上角用户菜单 | 下拉菜单出现，显示邮箱 + 「退出登录」|
| 2 | 点击「退出登录」 | 按钮变为「退出中...」→ 跳转到 `/login` |
| 3 | 按浏览器后退键 | **不应**回到 `/dashboard`（应被 middleware 拦截回 `/login`）|

### A10. Session 持久性

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 登录后关闭浏览器标签页 | — |
| 2 | 重新打开 `http://localhost:3000/dashboard` | 仍然登录状态（cookie 未过期）|

---

## 模块 B：Dashboard

### B1. Dashboard 页面加载

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 登录后访问 `/dashboard` | 页面正常渲染，7 个区块全部显示 |
| 2 | 检查内容：Banner、问候语、统计卡片、作文列表、热力图、活动面板 | 全部可见 |

**已知问题**：Dashboard 使用**静态 mock 数据**（`dashboard-data.ts`），不是从数据库读取。新注册用户也会看到「李同学」「12 篇作文」等假数据。这是 Phase 1 Step 3 的遗留，后续 Step 会接真实数据。

### B2. 侧边栏导航

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 点击「我的工坊」 | 高亮，跳转 `/dashboard` |
| 2 | 点击「新建作文」 | 跳转 `/editor` |
| 3 | 点击「作品详情」 | **⚠️ 404 — 该页面尚未创建** |
| 4 | 点击「批改请求」 | **⚠️ 404 — 该页面尚未创建** |
| 5 | 点击「广场」| 置灰不可点（disabled，Phase 2.5）|
| 6 | 点击「真题句子库」 | 置灰不可点（disabled，Phase 2）|

### B3. TopBar 操作

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 在 Dashboard 页面，检查 TopBar | 显示「我的工坊」标题 + 「+ 新建作文」按钮 |
| 2 | 点击「+ 新建作文」 | 跳转 `/editor` |
| 3 | 在 `/editor` 页面，检查 TopBar | 显示「作文编辑器」标题，无「新建作文」按钮 |

### B4. 作文列表点击

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 点击 Dashboard 中的任意作文 | **⚠️ 跳转 `/detail` → 404（页面未创建）** |

---

## 模块 C：作文编辑器（核心模块）

### C1. 新建作文 — 完整发布流程

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 从 Dashboard 点击「+ 新建作文」 | 跳转 `/editor`，编辑器加载空白内容 |
| 2 | 检查 TopBar 标题 | 显示「作文编辑器」|
| 3 | 在标题栏输入 `Test Essay` | — |
| 4 | 选择标签「高考作文」| — |
| 5 | 选择可见性「公开」| — |
| 6 | 在编辑器中输入英文内容，如 `This is a test essay about technology.` | 右下角 AI 面板实时扫描，字数统计更新 |
| 7 | 点击「发布」 | 按钮变为「发布中...」→ 跳转 `/dashboard` |
| 8 | 检查 Supabase → `essays` 表 | 新记录：title=Test Essay, tags=[gaokao], visibility=public, word_count=正确值 |
| 9 | 检查 Supabase → `essay_versions` 表 | 新记录：version_number=1, content=TipTap JSON |
| 10 | 检查 Supabase → `essay_members` 表 | 新记录：role=owner（触发器自动添加）|

### C2. 新建作文 — 空标题拦截

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 打开 `/editor`，不填标题 | — |
| 2 | 输入正文内容 | — |
| 3 | 点击「发布」 | 底部弹出红色提示「请输入作文标题」，不跳转 |

### C3. 新建作文 — 空内容拦截

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 打开 `/editor`，填标题 `Empty Test` | — |
| 2 | 不输入任何正文 | 字数显示 0 |
| 3 | 点击「发布」 | 底部弹出红色提示「作文内容不能为空」|

### C4. 草稿自动保存

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 打开 `/editor`，输入标题和正文 | — |
| 2 | 等待 2 秒 | 元数据栏出现「已自动保存 HH:MM」|
| 3 | 打开 DevTools → Application → Local Storage → `localhost:3000` | 看到 key `engforge-draft-new`，值为 JSON（含 title/tags/visibility/content/savedAt）|
| 4 | 刷新页面 | 顶部出现黄色提示「检测到 HH:MM 的未保存草稿，是否恢复？」|

### C5. 草稿恢复

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 在 C4 的基础上，点击「恢复草稿」 | 黄色提示消失，标题和正文恢复为草稿内容 |
| 2 | 点击「丢弃」替代恢复 | 黄色提示消失，编辑器清空，localStorage 中的 draft 被删除 |

### C6. 草稿 — 发布后清理

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 输入内容，等待草稿保存 | — |
| 2 | 点击「发布」成功跳转 | — |
| 3 | 再次打开 `/editor` | **不应**出现草稿恢复提示（localStorage 已清理）|

### C7. 编辑已有作文

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 从 Supabase 获取 C1 创建的 essay 的 UUID | — |
| 2 | 访问 `/editor/{UUID}` | 编辑器加载，标题和内容为 C1 发布的内容 |
| 3 | 修改正文内容 | — |
| 4 | 点击「发布新版本」 | 跳转 `/dashboard` |
| 5 | 检查 `essay_versions` 表 | 新增一条 version_number=2 的记录 |
| 6 | 检查 `essays` 表的 `word_count` | **⚠️ 检查是否更新为新版本的词数** |

### C8. 编辑作文 — 标题/标签修改

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 打开 `/editor/{UUID}` | — |
| 2 | 修改标题为 `Updated Title` | — |
| 3 | 修改标签为「CET-4」| — |
| 4 | 点击「发布新版本」 | 跳转 `/dashboard` |
| 5 | 检查 `essays` 表 | **⚠️ 潜在 BUG：标题和标签可能未更新（publishNewVersion 只发 content，不发 title/tags）** |

### C9. 编辑作文 — 无权限

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 用 testuser1 创建一篇作文（默认 private）| — |
| 2 | 退出，用 testuser2 登录 | — |
| 3 | 访问 `/editor/{testuser1的essayId}` | **预期**：显示「无编辑权限」或 404（RLS 阻止查看 private 作文）|

### C10. 编辑作文 — 不存在的 ID

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 访问 `/editor/00000000-0000-0000-0000-000000000000` | 404 页面（notFound() 触发）|

### C11. 富文本工具栏

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 选中一段文字，点击 **B**（加粗） | 文字变粗 |
| 2 | 选中一段文字，点击 *I*（斜体） | 文字变斜 |
| 3 | 光标在新行，点击 **H1** | 变为一级标题 |
| 4 | 点击 **H2** | 变为二级标题 |
| 5 | 点击有序列表按钮 | 生成编号列表 |
| 6 | 点击无序列表按钮 | 生成圆点列表 |
| 7 | 点击引用按钮 | 变为引用块样式 |
| 8 | 点击撤销 | 撤销上一步 |
| 9 | 点击重做 | 恢复撤销的操作 |

### C12. AI 标注面板

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 在编辑器中输入 `I think this is very good and important.` | — |
| 2 | 检查右侧 AI 面板 | 应扫描出 `think`、`very`、`good`、`important` 等词（取决于词库）|
| 3 | 点击某个建议词 | 编辑器中对应单词被替换，AI 面板更新 |
| 4 | 再次点击同一个建议词 | **检查**：如果文中已无匹配词，不应报错 |

### C13. AI 替换 — 同一词出现多次

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 输入 `I think you think we think` | — |
| 2 | 点击 `think` 的建议替换词 | 只有**第一个** `think` 被替换 |
| 3 | 再次点击 | 第二个被替换 |

### C14. 字数统计

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 输入纯英文 `Hello world test` | 字数 = 3 |
| 2 | 输入中英混合 `Hello 世界 test` | 字数 = 2（只计英文单词）或按实现逻辑检查 |
| 3 | 输入空格和换行 | 字数不变 |
| 4 | 输入标点符号 `Hello, world!` | 字数 = 2 |

---

## 模块 D：数据完整性

### D1. RLS — 用户隔离

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | testuser1 创建一篇 private 作文 | — |
| 2 | 退出，testuser2 登录 | — |
| 3 | 在 Supabase Dashboard 用 anon key 查询 essays | testuser2 **不应**看到 testuser1 的 private 作文 |

### D2. 版本号自增

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 对同一篇作文发布 3 次 | `essay_versions` 表中 version_number = 1, 2, 3 |

### D3. essay_members 触发器

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 创建新作文 | `essay_members` 自动添加一条 owner 记录 |
| 2 | 检查 user_id | 等于当前登录用户 ID |
| 3 | 检查 role | = `owner` |

### D4. profiles 触发器

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 新用户注册 | `profiles` 表自动创建记录 |
| 2 | 检查 username | = 注册时填写的用户名 |
| 3 | 检查 avatar_initials | = 用户名前两字母大写 |

---

## 模块 E：边界 & 异常

### E1. 超长标题

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 输入 200+ 字符的标题 | 检查数据库是否截断或报错 |
| 2 | 检查 Dashboard 列表显示 | 超长标题是否破坏布局 |

### E2. 大量内容

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 粘贴 5000+ 字的英文内容 | 编辑器不卡顿，字数统计正确 |
| 2 | 发布 | 检查 Supabase 是否成功存储（content 字段是 jsonb）|

### E3. 网络断开时发布

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 在编辑器中输入内容 | — |
| 2 | 打开 DevTools → Network → 切换到 Offline | — |
| 3 | 点击「发布」 | **预期**：显示错误提示，不跳转，不丢内容 |
| 4 | 恢复网络 | 草稿应仍在，可重新发布 |

### E4. 快速连续点击发布

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 输入内容后快速连续点击「发布」3 次 | **预期**：只创建 1 篇作文（按钮 disabled 状态应阻止）|

### E5. 浏览器后退

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 发布成功跳转到 `/dashboard` 后 | — |
| 2 | 按浏览器后退 | 回到 `/editor` 页面，编辑器应为空（不应残留已发布内容）|

### E6. 直接 URL 访问编辑器

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 未登录，直接输入 `http://localhost:3000/editor` | 重定向到 `/login` |
| 2 | 登录后直接输入 `/editor` | 正常加载空编辑器 |

---

## 模块 F：UI 一致性

### F1. 响应式 — 窗口缩小

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 在 Dashboard 页面，缩小窗口到 800px 宽 | 检查布局是否错位 |
| 2 | 在编辑器页面，缩小窗口 | 检查 AI 面板是否挤压编辑器 |

### F2. 控制台报错

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 走完所有页面（Landing → Login → Register → Dashboard → Editor）| Console 中**无红色 Error** |
| 2 | 检查 React DevTools 警告 | 无 "key" 警告、无 hooks 警告 |

---

## 已知问题汇总（代码审查发现）

以下是代码审查中发现的潜在 bug，测试时重点验证：

| # | 问题 | 位置 | 严重度 |
|---|------|------|--------|
| 1 | **publishNewVersion 不更新标题/标签/可见性** — 编辑模式下修改标题后发布新版本，essays 表的 title 不会更新 | `essay-actions.ts` L125-163 | 🔴 高 |
| 2 | **Dashboard 全部是静态 mock 数据** — 新用户也看到「李同学」「12篇作文」| `dashboard-data.ts` | 🟡 中（已知，Step 3 遗留）|
| 3 | **注册成功提示用红色错误样式** — 语义不对，应绿色 | `register/page.tsx` L46-48 | 🟡 中 |
| 4 | **Dashboard 作文列表链接到 `/detail`** — 该路由不存在，点击 404 | `dashboard-data.ts` L147,153,163,172,181 | 🟡 中 |
| 5 | **侧边栏「作品详情」「批改请求」链接到不存在的路由** | `dashboard-data.ts` L110-112 | 🟡 中 |
| 6 | **草稿 key 碰撞** — 新建作文的 draft key 是 `engforge-draft-new`，多用户共用浏览器会互相覆盖 | `EditorClient.tsx` L98 | 🟢 低 |
| 7 | **middleware 文件名约定已废弃** — Next.js 16 提示应改为 `proxy` | `middleware.ts` | 🟢 低（功能正常）|
