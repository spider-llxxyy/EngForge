# EngForge Phase 1 测试流程

> 目标：系统性走完所有用户路径，找出功能漏洞、数据不一致、边界崩溃。
> 方法：按模块逐个测试，每个测试用例有明确步骤和预期结果。
> 工具：浏览器 + Supabase Dashboard（看数据）+ 浏览器 DevTools Console（看报错）
>
> **覆盖范围**：Phase 1 全部功能（Step 1-11）：认证 / Dashboard / 编辑器 / Fork 与版本 / PR 系统 / 通知 / 邀请协作
> **建议测试顺序**：A → B → C → D → E → F → G → H → I → J（后四个模块需要两个测试账号配合）

---

## 准备工作

1. 确保 dev server 在运行：`http://localhost:3000`
2. 打开浏览器 DevTools（F12），切到 Console 和 Network 面板
3. 打开 Supabase Dashboard → Table Editor，准备查看数据库变化
4. 准备两个测试邮箱（如 test1@gmail.com / test2@gmail.com），下文称 **用户A**（作文主人）/ **用户B**（协作者）
5. 建议用两个浏览器窗口（或普通+无痕）分别登录 A/B，方便测通知和协作

---

## 模块 A：认证系统

### A1. 注册 — 邮箱确认关闭模式

> 前提：Supabase Dashboard → Authentication → Providers → Email → Confirm email = OFF

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 打开 `http://localhost:3000/register` | 看到注册表单 |
| 2 | 填入用户名 `testuser1`、邮箱、密码 `123456` | — |
| 3 | 点击「注册」 | 按钮变为「注册中...」→ 5 秒倒计时后自动跳转 `/login` |
| 4 | 检查 Supabase → Table Editor → `profiles` 表 | 应有一条新记录，username = testuser1，avatar_initials = TU |
| 5 | 检查 Console | 无报错 |

**潜在 bug**：如果触发器 `handle_new_user` 失败，auth.users 有记录但 profiles 没有 → 此时 layout.tsx 的 `getSessionUser()` 会 fallback 到 email 前缀，但 avatar_initials 会变成 "U"

### A2. 注册 — 重复邮箱

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 用 A1 同样的邮箱再次注册 | 显示「该邮箱已被注册」（identities.length===0 判重）|
| 2 | 检查 Console | 无未捕获异常 |

### A3. 注册 — 密码过短

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 填入密码 `123`（少于 6 位） | 浏览器原生校验阻止提交（minLength=6）|

### A4. 注册 — 邮箱确认开启模式

> 前提：Supabase Dashboard → Confirm email = ON
> 同时确认：Authentication → URL Configuration → Site URL = `http://localhost:3000`，Redirect URLs 包含 `http://localhost:3000/auth/callback`

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 用新邮箱注册 | 显示绿色成功提示「注册成功！请查收确认邮件后登录。」+ 5 秒倒计时跳转 |
| 2 | 去邮箱查收 | 收到 Supabase 确认邮件 |
| 3 | 点击邮件中的链接 | 跳转到 `http://localhost:3000/auth/callback` → 自动跳转 `/login?verified=1` → 登录页显示绿色「邮箱确认成功」提示 |
| 4 | 如果 code_verifier 丢失（换浏览器点链接） | 跳转到 `/login?verified=1`（绿色提示），不报红色错误 |

### A5. 登录 — 正常流程

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 退出登录，回到 `/login` | 看到登录表单 |
| 2 | 填入 A1 注册的邮箱和密码 | — |
| 3 | 点击「登录」 | 跳转到 `/dashboard` |
| 4 | 检查 TopBar 右上角 | 显示头像缩写 + 用户名 + 铃铛 + Ticket 图标 |

### A6. 登录 — 错误密码 / 速率限制

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 填入正确邮箱，错误密码 | 显示「邮箱或密码错误」（不暴露 Supabase 原始错误）|
| 2 | 连续失败多次触发 429 | 显示速率限制中文提示，按钮冷却 60 秒 |
| 3 | 网络挂掉/请求超 10 秒 | 显示超时提示，不白屏 |

### A7. 路由保护 — 未登录访问受保护页面

> 注：middleware 已迁移为 Next.js 16 的 `src/proxy.ts`，行为应完全一致。

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 退出登录，直接访问 `/dashboard` | 307 重定向到 `/login?redirect=/dashboard` |
| 2 | 直接访问 `/editor` | 重定向到 `/login?redirect=/editor` |
| 3 | 直接访问 `/editor/任意ID`、`/essays/任意ID` | 重定向到 `/login` |

### A8. 路由保护 — 已登录访问认证页

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 登录后访问 `/login` | 重定向到 `/dashboard` |
| 2 | 登录后访问 `/register` | 重定向到 `/dashboard` |

### A9. 退出登录

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 点击右上角用户菜单 | 下拉菜单出现，显示邮箱 + 「退出登录」|
| 2 | 点击「退出登录」 | 按钮变为「退出中...」→ 跳转到 `/login` |
| 3 | 按浏览器后退键 | **不应**回到 `/dashboard`（应被 proxy 拦截回 `/login`）|

### A10. Session 持久性

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 登录后关闭浏览器标签页 | — |
| 2 | 重新打开 `http://localhost:3000/dashboard` | 仍然登录状态（cookie 未过期）|

---

## 模块 B：Dashboard

### B1. Dashboard 页面加载（真实数据）

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 新注册用户登录后访问 `/dashboard` | 问候语显示自己的用户名，作文列表为空态，统计全 0 |
| 2 | 检查 7 个区块：Banner、问候语、统计卡片、作文列表、热力图、活动面板 | 全部可见，无「李同学」等 mock 数据 |
| 3 | 发布一篇作文后刷新 | 统计「我的作文」= 1，列表出现该作文，热力图今天格子变色 |

### B2. 侧边栏导航

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 点击「我的工坊」 | 高亮，跳转 `/dashboard` |
| 2 | 点击「新建作文」 | 跳转 `/editor` |
| 3 | 点击「批改请求」 | 置灰不可点（disabled，Phase 2）|
| 4 | 点击「广场」| 置灰不可点（disabled，Phase 2.5）|
| 5 | 点击「真题句子库」 | 置灰不可点（disabled，Phase 2）|

> 注：「作品详情」当前指向 `/dashboard`（占位链接，非 404）——已知待 Phase 2 处理。

### B3. TopBar 操作

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 在 Dashboard 页面，检查 TopBar | 显示「我的工坊」标题 + 「+ 新建作文」按钮 |
| 2 | 点击「+ 新建作文」 | 跳转 `/editor` |
| 3 | 在 `/editor`、`/essays/[id]`、`/essays/[id]/prs/[prId]` 页面 | TopBar 标题分别为「作文编辑器」/ 作文标题 / 「批改请求」 |

### B4. 作文列表点击

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 点击 Dashboard 中的任意作文 | 跳转 `/essays/{id}`，正常显示详情页 |

### B5. 统计卡片（含连续天数）

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 今天发布过作文/新版本 | 「连续天数」= 1（或与历史连续）|
| 2 | 昨天有活动、今天还没有 | 「连续天数」仍显示昨天的连续数（今天未打破）|
| 3 | 隔了一整天没活动 | 「连续天数」归零，sub 显示「今天写一篇」|

---

## 模块 C：作文编辑器（核心模块）

### C1. 新建作文 — 完整发布流程

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 从 Dashboard 点击「+ 新建作文」 | 跳转 `/editor`，编辑器加载空白内容 |
| 2 | 在标题栏输入 `Test Essay`，选标签「高考作文」、可见性「公开」 | — |
| 3 | 输入英文内容 `This is a test essay about technology.` | 右侧 AI 面板实时扫描，字数统计更新 |
| 4 | 点击「发布」 | 按钮变为「发布中...」→ toast 成功 → 跳转 `/dashboard` |
| 5 | 检查 Supabase → `essays` 表 | 新记录：title/tags/visibility/word_count 正确 |
| 6 | 检查 `essay_versions` 表 | version_number=1, content=TipTap JSON |
| 7 | 检查 `essay_members` 表 | 新记录 role=owner（触发器自动）|

### C2/C3. 空标题 / 空内容拦截

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 不填标题点「发布」 | 提示「请输入作文标题」，不跳转 |
| 2 | 填标题不填正文点「发布」 | 提示「作文内容不能为空」|

### C4-C6. 草稿自动保存 / 恢复 / 清理

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 输入标题和正文，等 2 秒 | 元数据栏出现「已自动保存 HH:MM」|
| 2 | DevTools → Application → Local Storage | key 为 `engforge-draft-{userId}-new`（**含 userId**）|
| 3 | 刷新页面 | 顶部黄色提示「检测到 HH:MM 的未保存草稿，是否恢复？」|
| 4 | 点「恢复」 | 标题和正文恢复；点「丢弃」则清空并删除草稿 |
| 5 | 发布成功后再开 `/editor` | 不出现草稿恢复提示 |

**多账号隔离**：用户A 存了草稿 → 退出 → 用户B 登录开 `/editor` → **不应**看到 A 的草稿恢复提示。

### C7. 编辑已有作文

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 从 Detail 页点「编辑」进 `/editor/{id}` | 加载当前版本内容 |
| 2 | 修改正文 **和标题/标签**，点「发布新版本」 | `essay_versions` 新增 v2；`essays` 表 title/tags/word_count **同步更新** |
| 3 | 跳转回 Detail 页 | 标题已更新 |

### C8. 编辑作文 — 无权限

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 用户B 访问用户A 的 private 作文 `/editor/{A的id}` | 显示「无编辑权限」锁提示 |

### C9. 编辑作文 — 不存在的 ID

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 访问 `/editor/00000000-0000-0000-0000-000000000000` | 404 页面 |

### C10. 富文本工具栏

加粗/斜体/H1/H2/有序列表/无序列表/引用/撤销/重做——逐个点击验证样式生效、撤销重做正常。

### C11-C13. AI 标注面板 / 多次替换 / 字数统计

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 输入 `I think this is very good and important.` | AI 面板扫描出 think/very/good/important 等建议 |
| 2 | 点建议词 | 编辑器中**第一个**匹配词被替换，面板更新 |
| 3 | 输入 `I think you think we think` 连续替换 | 逐个替换，不报错 |
| 4 | 纯英文 `Hello world test` / `Hello, world!` | 字数 = 3 / 2 |

---

## 模块 D：数据完整性

### D1. RLS — 用户隔离

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 用户A 创建 private 作文；用户B 登录 | B 的 Dashboard **看不到** A 的作文 |
| 2 | B 直接访问 `/essays/{A的private id}` | 404（RLS 阻止）|
| 3 | A 把作文改为 public | B 可访问详情、可 Fork、可 Star，但**无编辑入口** |

### D2. 版本号自增

对同一篇作文发布 3 次 → `essay_versions` 中 version_number = 1, 2, 3。

### D3. 触发器

新建作文 → `essay_members` 自动 owner 记录；新用户注册 → `profiles` 自动记录。

---

## 模块 E：边界 & 异常

| # | 操作 | 预期结果 |
|---|------|----------|
| E1 | 200+ 字符超长标题 | 数据库截断或报错有提示，列表布局不破 |
| E2 | 粘贴 5000+ 字内容 | 编辑器不卡顿，发布成功（jsonb 存储）|
| E3 | DevTools Network → Offline 后点发布 | 显示错误提示，不跳转，**内容不丢**（草稿仍在）|
| E4 | 快速连点「发布」3 次 | 只创建 1 篇（按钮 disabled 阻止）|
| E5 | 发布跳转后按浏览器后退 | 编辑器为空，不残留已发布内容 |
| E6 | 未登录直接输 `/editor` URL | 重定向 `/login` |

---

## 模块 F：UI 一致性

| # | 操作 | 预期结果 |
|---|------|----------|
| F1 | 窗口缩到 800px 宽（Dashboard / 编辑器 / Detail）| 布局无严重错位（完整响应式是 P3 债务，当前只要求不破）|
| F2 | 走完全部页面 | Console 无红色 Error，无 React key/hooks 警告 |

---

## 模块 G：Fork 与版本管理（Step 7）

> 前置：用户A 有一篇 public 作文。

### G1. Fork 基本流程

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 用户B 打开 A 的公开作文 Detail 页 | 显示 Fork 按钮 |
| 2 | 点击「Fork」 | 按钮加载态 → toast 成功 → 跳转到**自己工坊的新作文** |
| 3 | 检查 `essays` 表 | 新记录 author_id=B，forked_from=A 的 id |
| 4 | 检查 A 的作文 | fork_count +1 |
| 5 | 用户A 的铃铛 | 收到「作文被 Fork」通知（见模块 I）|

### G2. Fork 独立性

B 在 Fork 副本上发布修改 → A 的原作内容/版本**不受影响**。

### G3. 版本切换预览

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 对一篇有 2+ 版本的作文，在 Detail 页打开「版本历史」 | 列出所有版本（号、时间、摘要）|
| 2 | 点击旧版本 | 正文区域切换为该版本内容（预览态，有提示）|
| 3 | 切回最新 | 恢复显示最新版本 |

### G4. 恢复历史版本

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 预览旧版本时点「恢复此版本」 | 确认后创建**新版本**（版本号 = max+1），内容回到旧版 |
| 2 | 检查 `essay_versions` | 新增记录，而非删除后续版本 |

### G5. Star / 取消 Star

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 用户B 对 A 的公开作文点 Star | star_count +1，按钮变已收藏态 |
| 2 | 再点一次 | 取消收藏，star_count -1 |
| 3 | B 对不存在的 essayId 调 Star（控制台模拟） | Server Action 返回错误（view 校验拦截）|

---

## 模块 H：PR 系统（Step 8）

> 前置：用户A 的作文已通过邀请码把 B 加为 editor 成员（先做模块 J1-J3）。

### H1. 提交批改（创建 PR）

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 用户B 打开该作文 Detail 页，「批改记录」tab | 可见「提交批改」按钮（editor/owner 可见）|
| 2 | 点击进入 `/essays/{id}/prs/new` | TipTap 预载当前版本内容 |
| 3 | 修改内容 + 填 PR 标题/描述，提交 | toast 成功 → 跳转 PR 详情页 |
| 4 | 检查 `essay_versions` | 自动生成 head 版本（latest_version 更新，current_version **不变**）|
| 5 | 检查 `pull_requests` 表 | 新记录 status=open，base/head 版本 id 正确 |
| 6 | 用户A 的铃铛 | 收到「新批改请求」通知 |

### H2. 查看 diff

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 打开 PR 详情页 | diff 视图：新增行**绿色背景**，删除行**红色背景 + 删除线** |
| 2 | 与实际修改对比 | 增删行准确（LCS 行级 diff），无遗漏/错位 |

### H3. 合并 PR

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 用户A（owner）点「合并」 | 确认弹窗 → 加载态 → 成功 |
| 2 | 检查 `pull_requests` | status=merged |
| 3 | 检查 `essay_versions` / `essays` | 生成新版本且 current_version 指向它 |
| 4 | 回到作文 Detail 页 | 正文已是合并后内容 |
| 5 | 用户B 的铃铛 | 收到「批改已合并」通知 |

### H4. 关闭 PR

点「关闭」→ status=closed，**不**生成新版本，正文不变。

### H5. 权限边界

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 非成员用户B2 访问 `/essays/{A的id}/prs/new` | 被拦截（view 校验）|
| 2 | 非 owner 尝试合并（用户B 点合并，若有按钮） | Server Action 拒绝并提示错误 |

---

## 模块 I：通知系统（Step 9）

### I1. 触发与徽章

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 触发任一事件（被 Fork / 收到 PR / PR 被合并 / 成员加入）| 铃铛徽章 +1（首帧就有，无闪烁）|
| 2 | 点铃铛 | 下拉面板显示最近 10 条：图标按类型着色、相对时间、未读高亮点 |

### I2. Realtime 实时推送（重点）

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 用户A 页面保持打开（不刷新）| — |
| 2 | 用户B 在另一窗口 Fork A 的作文 | A **无需刷新**：徽章 +1 + 右下角 sonner toast 弹出 |
| 3 | 点 toast「查看」 | 跳转到对应 link_url |

### I3. 已读状态

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 点某条通知 | 该条标已读（去高亮）+ 跳转 link_url |
| 2 | 点「全部已读」 | 徽章清零，所有条目去高亮 |
| 3 | 检查 `notifications` 表 | is_read=true 已持久化（刷新后仍已读）|

### I4. 边界

| # | 操作 | 预期结果 |
|---|------|----------|
| 1 | 制造 100+ 未读 | 徽章显示 99+ 封顶 |
| 2 | A 页面断网 30 秒再恢复 | 恢复后新事件继续推送（断线窗口内的靠刷新补齐——已知 P3 限制）|

---

## 模块 J：邀请协作（Step 10）

### J1. 生成邀请码

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 用户A 打开自己作文的 Detail 页侧栏「协作成员」 | 显示 InviteManager 入口 |
| 2 | 点「生成邀请码」 | 弹窗内新码出现，`invite_codes` 表新记录，默认 7 天有效、不限次数 |

### J2. 凭码加入

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 用户B 点 TopBar 的 Ticket 图标 | 弹出输入邀请码 modal |
| 2 | 输入 J1 的码，提交 | toast 成功 → 跳转该作文 → 侧栏成员列表出现 B（editor）|
| 3 | 用户A 的铃铛 | 收到「成员加入」通知（实时）|

### J3. 幂等与失效

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | B 用**同一码**再次加入 | 幂等处理：不报错、不重复加成员（`use_invitation` ON CONFLICT DO NOTHING）|
| 2 | A 撤销该码后再有人用 | 加入失败，提示码已失效 |
| 3 | 构造过期码（Supabase 把 expires_at 改成过去）| 加入失败，提示已过期 |

### J4. 成员管理

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | A 在成员列表点 B 的「移除」| 出现内联确认 → 确认后 B 从列表消失 |
| 2 | B 再访问该作文编辑页 | 失去编辑权（锁提示或 404）|
| 3 | 非 owner（用户B）看侧栏 | **无**邀请码管理入口、**无**移除成员按钮 |

---

## 已知问题与遗留（2026-08-15 复核）

### 已修复（无需再测，抽查即可）

| # | 原问题 | 修复位置 |
|---|--------|----------|
| 1 | publishNewVersion 不更新标题/标签 | essay-actions.ts 已更新元数据 |
| 2 | Dashboard 全部 mock 数据 | page.tsx 已接真实数据（热力图/活动/列表/统计）|
| 3 | 注册成功提示红色样式 | 已改绿色 |
| 4 | Dashboard 链接 404（/detail）| Step 7 已改 `/essays/[id]` |
| 5 | 草稿 key 多用户碰撞 | Step 11 已拼入 userId |
| 6 | middleware 弃用警告 | Step 11 已迁移 `src/proxy.ts` |
| 7 | 连续天数假数据 | Step 11 已真实计算 |

### 仍开放的已知限制（测试时按此预期，不算 bug）

| # | 限制 | 计划 |
|---|------|------|
| 1 | AI 标注仅客户端 11 词词库 | Phase 3 接 LLM |
| 2 | PR diff 仅纯文本 | Phase 2 |
| 3 | editor 成员发布新版本时，essays 元数据更新被 RLS 静默跳过（版本内容正常）| Phase 2 |
| 4 | use_invitation 幂等但 used_count 仍 +1 | Phase 2 观察 |
| 5 | Realtime 断线窗口内漏推送，靠刷新补齐 | P3 |
| 6 | 无完整响应式适配 | P3 |
| 7 | 邀请码仅支持 essay 级，不支持注册级 | Phase 2 |
